version: 2.1

jobs:
  test:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - run:
          name: Run Maven tests
          command: mvn -q test

workflows:
  test_workflow:
    jobs:
      - test
